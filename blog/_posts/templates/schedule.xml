<schedule>
    <conference>
        <acronym>pyconau-2019</acronym>
        <title>PyCon Au 2019</title>
        <subtitle>Extended</subtitle>
        <venue>Internal Convention Centre, Sydney, AU</venue>
        <start>2019-08-02</start>
        <end>2019-08-04</end>
        <days>3</days>
        <timeslot_duration>01:00</timeslot_duration>
    </conference>
    {% for idx, (date, rooms) in enumerate(days.items()) %}
    <day date="{{date}}" index="{{idx}}">
        {% for room, talks in rooms %}
        <room name="{{room}}">
            {% for talk in talks %}
            <event id="{{talk.conf_key}}">
                <slug/>
                <date>{{talk.start}}</date>
                <start>{{ fromisoformat(talk.start).time().isoformat() }}</start>
                <duration>{{ to_time(talk.duration) }}</duration>
                <room>{{talk.room}}</room>
                <title>{{talk.name}}</title>
                <subtitle/>
                <track>{{talk.track}}</track>
                <type>{{talk.type}}</type>
                <language/>
                <description/>
                <abstract>{{talk.abstract}}</abstract>
                <persons>
                    {% for author in talk.authors %}
                    <person>{{author}}</person>
                    {% endfor %}
                </persons>
                <links>
                    <link href="{{ talk.conf_url }}">
                        Conference link
                    </link>
                    {% for twitter in talk.twitter_ids %}
                        {% if twitter %}
                            <link href="https://twitter.com/{{twitter}}">
                                @{{twitter}}
                            </link>
                        {% endif %}
                    {% endfor %}
                </links>
            </event>
            {% endfor %}
        </room>
        {% endfor %}
    </day>
    {% endfor %}
</schedule>
